# 飞书知识库生产线 — Claude 项目说明

## 项目概述

基于飞书 + Claude Code 的专题知识库自动化生产系统。将外部资料经 AI 结构化处理后写入飞书多维表格，再聚合升级为专题知识库成品页面。

**GitHub 仓库**：`xiwenran/lark-knowledge`

---

## 核心架构

```
外部资料 → AI结构化处理 → 飞书多维表格 → 聚合升级 → 专题知识库成品
         (收件入表)                    (升级为页面)
```

**两段式工作流**：
1. **收件入表**：外部资料经 AI 提取关键信息后写入多维表格（结构化存储）
2. **升级成品**：从表格中筛选高价值记录，聚合生成知识库页面（内容产出）

**三层分类体系**：专题 → 资产形态 → 具体条目

---

## 当前专题

| 专题 | 说明 |
|------|------|
| 小红书 | 小红书运营相关知识 |
| 虚拟资料产品 | 虚拟资料类产品知识 |
| AI编程 | AI 辅助编程相关知识 |

---

## 目录结构

```
lark-knowledge/
├── CLAUDE.md        # 本文件：项目说明 + 开发约定
├── memory/          # Claude Code 记忆（MEMORY.md + 业务背景 + 搭建进度）
├── skills/          # 5 个 lark-knowledge-* skill
│   ├── lark-knowledge-intake/    # 收件入表
│   ├── lark-knowledge-upgrade/   # 升级为页面
│   ├── lark-knowledge-format/    # 排版美化
│   ├── lark-knowledge-sync/      # 同步规范
│   └── lark-knowledge-lint/      # 巡检扫描
├── scripts/         # sync-knowledge.sh 月度同步脚本
└── logs/            # 自动任务日志
```

---

## 核心 Skill 一览

| Skill | 触发词 | 功能 |
|-------|--------|------|
| `lark-knowledge-intake` | `/收件` | 资料 → 结构化 → 写入多维表格 |
| `lark-knowledge-upgrade` | `/升级` | 表格记录 → 聚合 → 知识库页面 |
| `lark-knowledge-format` | `/排版` | 文档 → 彩色富文本排版美化 |
| `lark-knowledge-sync` | `/同步规范` | config.json 字段定义 → 飞书规范文档 |
| `lark-knowledge-lint` | `巡检` | 扫描积压/孤岛/矛盾，输出巡检报告 |

---

## 排版规范（v6.4.3）

- 每句话 2–4 个着色词（不多不少）
- 用 Callout 替代普通列表，提升视觉层次
- 标题用 text color 包裹，统一色彩风格
- **铁律：排版只加视觉格式，绝对不改文字内容**

---

## 开发约定

### 配置管理
- `config.json` 统一管理飞书 token、表格 ID、知识库空间 ID 等
- **禁止**在 skill 代码中硬编码飞书凭证或资源 ID

### 工作流规范
- 严格遵循两段式工作流：先收件入表，再升级为知识库页面
- 收件时必须完成 AI 结构化提取，不允许原文直接灌入
- 升级时按资产形态模板生成正文，自动调用排版美化

### 内容安全
- 排版不改文字（只增加颜色、callout、标题样式，绝不修改原始文字）
- 推送前检查是否包含敏感信息（token、个人路径等）

### 分类体系
- 新条目必须归入：专题 → 资产形态 → 具体条目 三层结构
- 新增专题需同步更新 config.json 和飞书规范文档

---

## ⚡ Echo 会话检查清单（每次任务完成前必查）

完成任何实质性改动后，**在报告完成之前**，按顺序检查：

**① 高风险改动？→ 冷眼审查**
- 触发条件：发布逻辑 / 写入文件或数据库 / 防重复机制
- 操作：说"我将触发冷眼审查"，用 Agent 派独立子代理只看 diff + 需求

**② 用户表达不确定？→ 建议圆桌讨论**
- 触发条件："要不要做X"、"感觉……但不确定"、"先做A还是B"
- 操作：说"这是方向性问题，建议三省讨论"，提示用户输入「圆桌讨论：[问题]」

**③ 有实质改动？→ Obsidian 捕获**
- 触发条件：新功能完成 / 重构 / bug 根因找到 / 推送 GitHub
- 操作：展示 changelog 草稿，等用户确认后写入 `~/Obsidian/PersonalWiki/项目/lark-knowledge/changelog/`

**④ git push 前 → 脱敏扫描**
- 检查：`/Users/用户名/`绝对路径、token、邮箱、AppID
- 发现敏感内容：立即停下告知用户，修改后再推
